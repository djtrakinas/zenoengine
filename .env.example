# ==========================================
# 1. KONFIGURASI SERVER
# ==========================================
# Port aplikasi berjalan
APP_PORT=:3000

# Mode Aplikasi
# development = Tampilkan error detail di browser (Cocok saat coding)
# production  = Sembunyikan error, tampilkan pesan umum (Cocok saat live)
APP_ENV=development

# Rate Limiting
# Jumlah request maksimum per jendela waktu
RATE_LIMIT_REQUESTS=100
# Jendela waktu dalam detik (60 = 1 menit)
RATE_LIMIT_WINDOW=60

# Request Timeout (Context-Aware Executor)
# Batas waktu maksimal eksekusi HTTP request untuk mencegah infinite loop
# Format: durasi string (contoh: "30s", "1m", "2m30s")
ZENO_REQUEST_TIMEOUT=30s


# ==========================================
# 2. DATABASE (MySQL / SQLite / Postgres)
# ==========================================
# Pilihan Driver: mysql, sqlite, postgres, sqlserver
DB_DRIVER=mysql

# Format: IP:PORT
DB_HOST=127.0.0.1:3306
DB_USER=root
DB_PASS=
DB_NAME=zeno_app
# Config Pool Database
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# ==========================================
# 3. KEAMANAN (JWT & CSRF)
# ==========================================
# Kunci rahasia untuk tanda tangan token. 
# Run 'zeno key:generate' to set this automatically in your local .env
JWT_SECRET=
CSRF_TOKEN=your_csrf_token_here

# ==========================================
# 4. EMAIL SERVICE (SMTP)
# ==========================================
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# ==========================================
# 5. DATABASE TAMBAHAN (Multi-Database Support)
# ==========================================
# Mesin akan otomatis mendeteksi DB tambahan jika ada variable berawalan DB_[NAME]_ 
# dengan akhiran _DRIVER, _HOST, _NAME, _USER, atau _PASS.
# Database ini bisa dipanggil di ZenoLang menggunakan db: name

# Contoh 1: MySQL (Gudang)
# DB_GUDANG_DRIVER=mysql
# DB_GUDANG_HOST=127.0.0.1:3306
# DB_GUDANG_USER=root
# DB_GUDANG_PASS=
# DB_GUDANG_NAME=db_warehouse

# Contoh 2: PostgreSQL (Analytics)
# DB_ANALYTICS_DRIVER=postgres
# DB_ANALYTICS_HOST=127.0.0.1:5432
# DB_ANALYTICS_USER=postgres
# DB_ANALYTICS_PASS=password
# DB_ANALYTICS_NAME=analytics_db

# Contoh 3: SQLite (CMS)
# DB_ZENOCMS_DRIVER=sqlite
# DB_ZENOCMS_NAME=./zenocms.db

# ==========================================
# 6. TUTORIAL MAX DEMO (SQLite)
# ==========================================
# Digunakan oleh src/tutorial/max/
DB_TUTORIAL_DRIVER=sqlite
DB_TUTORIAL_NAME=./tutorial_max.db

# ==========================================
# 7. REDIS (Distributed Cache)
# ==========================================
REDIS_HOST=localhost:6379
REDIS_PASS=
REDIS_DB=0

# ==========================================
# 8. WORKER & SECURITY
# ==========================================
WORKER_ENABLED=false

# CSP Allowed Domains (dipisah spasi)
CSP_ALLOWED_DOMAINS="https://code.jquery.com https://unpkg.com"

# ==========================================
# 9. MULTI-TENANT EXAMPLES
# ==========================================
# DB_SYSTEM_DRIVER=mysql
# DB_SYSTEM_HOST=127.0.0.1:3306
# DB_SYSTEM_NAME=pos_system

# DB_TENANT_ABC_DRIVER=mysql
# DB_TENANT_ABC_NAME=pos_tenant_abc
