# ZenoEngine Configuration
# This file is loaded automatically at startup (like Go's godotenv)

# --- Application ---
# development: enables watcher, live reload, and verbose logs
# production: optimized for performance
APP_ENV=development
APP_PORT=:3000

# Timeout for HTTP requests (e.g., 30s, 1m)
ZENO_REQUEST_TIMEOUT=30s

# Validation level
# set to true only if you have many scripts and want to skip pre-flight syntax check
ZENO_SKIP_VALIDATION=false

# Background Worker
WORKER_ENABLED=true

# --- Security ---
# Used for JWT signing and session encryption
# Run 'zeno key:generate' to set this automatically in your .env
JWT_SECRET=

# --- Primary Database ---
# Supported drivers: mysql, sqlite, postgres, sqlserver
DB_DRIVER=sqlite
DB_NAME=zeno_app.db

# Required for MySQL/Postgres/SQLServer:
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=

# Connection Pool settings
DB_MAX_OPEN_CONNS=25
DB_MAX_IDLE_CONNS=5

# --- Additional Databases (Optional) ---
# ZenoEngine auto-detects variables with DB_{NAME}_HOST pattern
# DB_EXTERNAL_DRIVER=mysql
# DB_EXTERNAL_HOST=127.0.0.1
# DB_EXTERNAL_USER=root
# DB_EXTERNAL_PASS=
# DB_EXTERNAL_NAME=external_db

# --- Mail Services (SMTP) ---
# Used in ZenoLang via mail.send slot
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
